// Hotbar = placeable blocks only. Weapons/tools now live in the bag (gear.js).
import { B } from './blocks.js';

export const HOTBAR = [
  B.CONCRETE, B.GLASST, B.GLASSG, B.STEEL, B.MARBLE, B.NEON, B.NEONP,
  B.BRICK, B.GLASS, B.STONE, B.WOOD, B.PLANKS, B.LAMP,
];

export const DEFAULT_SLOT = 0;
